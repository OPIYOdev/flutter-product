name: grok_chat_template
description: A production-ready Flutter AI Chat App Template powered by xAI Grok API. Sell or customize for clients.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.2.2
  flutter_markdown: ^0.6.22
  uuid: ^4.3.3
  intl: ^0.19.0
  flutter_dotenv: ^5.1.0
  provider: ^6.1.5+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

